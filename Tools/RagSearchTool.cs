// RagSearchTool is no longer registered as an agent tool.
// RAG retrieval is now performed automatically in ReActAgent.BuildTaskPrompt
// using the pre-built KnowledgeBase, so no manual tool call is needed.
